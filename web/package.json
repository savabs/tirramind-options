{
  "name": "tirramind-options-web",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "dev": "wrangler pages dev public --compatibility-date=2026-09-01",
    "deploy": "wrangler pages deploy public --project-name=tirramind-options"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.8",
    "wrangler": "^3.99.0"
  }
}
